import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    height: 30,
    width: 100,
    backgroundColor: 'red',
    alignItems: 'center',
    justifyContent: 'center',
    borderWidth: 1
  }
});